---
    const phrase = Astro.props.phrase;
---
<section class="bridge-phrase">
    <p>{phrase}</p>
</section>

<style lang="scss">
    .bridge-phrase {
        display: flex;
        align-items: center;
        justify-content: center;
        p {
            text-transform: uppercase;
            font-size: 33px;
            letter-spacing: 1px;
        }
    }
</style>